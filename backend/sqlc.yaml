version: "2"
sql:
  - engine: "postgresql"
    queries: "./internal/repo/queries.sql"
    schema: "./internal/repo/migrations"
    gen:
      go:
        package: "repo"
        out: "./internal/repo"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_interface: true
